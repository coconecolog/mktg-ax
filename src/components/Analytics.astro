---
// GA4 / Microsoft Clarity。環境変数が未設定のときは何も出力しません。
// 値は .env（ローカル）または GitHub Secrets（本番）の PUBLIC_GA4_ID / PUBLIC_CLARITY_ID に設定してください。
// Search Console はタグではなくmeta認証（SEO.astro側）で対応しています。
import { GA4_ID, CLARITY_ID } from "@/consts";
---

{
  GA4_ID && (
    <>
      <script async is:inline src={`https://www.googletagmanager.com/gtag/js?id=${GA4_ID}`}></script>
      <script is:inline define:vars={{ GA4_ID }}>
        window.dataLayer = window.dataLayer || [];
        function gtag() {
          dataLayer.push(arguments);
        }
        gtag("js", new Date());
        gtag("config", GA4_ID);
      </script>
    </>
  )
}

{
  CLARITY_ID && (
    <script is:inline define:vars={{ CLARITY_ID }}>
      (function (c, l, a, r, i, t, y) {
        c[a] =
          c[a] ||
          function () {
            (c[a].q = c[a].q || []).push(arguments);
          };
        t = l.createElement(r);
        t.async = 1;
        t.src = "https://www.clarity.ms/tag/" + i;
        y = l.getElementsByTagName(r)[0];
        y.parentNode.insertBefore(t, y);
      })(window, document, "clarity", "script", CLARITY_ID);
    </script>
  )
}
